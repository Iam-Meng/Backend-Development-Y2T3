-- Create database if not exists
CREATE DATABASE IF NOT EXISTS sequelize_db;

-- Use the database
USE sequelize_db;

-- Sequelize will auto-create tables on sync
-- But we can pre-populate with sample data

-- Note: Tables will be created by Sequelize models on first run
-- This script just creates the database
